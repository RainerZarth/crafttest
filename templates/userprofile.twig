{% extends '_layout' %}
{% block main %}
<title>Nutzerprofil bearbeiten</title>
</head>
<body>
	{% include 'menu' %}
<div class="container">
	<div class="row">
		<div class="col s6">
			<form method="post" accept-charset="UTF-8">
				{{ csrfInput() }}
				<input type="hidden" name="action" value="users/save-user">
				{{ redirectInput('users/'~currentUser.username) }}
				<input type="hidden" name="userId" value="{{ currentUser.id }}">
				{# <label for="location">Location</label>
				funktioniert i d aktuellen version nicht
				<input type="text" id="location" name="fields[location]" value="{{ currentUser.location }}"> #}
				<label for="first-name">Vorname</label>
				<input type="text" id="first-name" name="firstName" value="{{ currentUser.firstName }}">
				
				<label for="last-name">Nachname</label>
				<input type="text" id="last-name" name="lastName" value="{{ currentUser.lastName }}">
				
				{# <label for="bio">Bio</label>
				funktioniert in der aktuellen version auch noch nicht
				<textarea id="bio" name="fields[bio]">{{ currentUser.bio }}</textarea> #}
				<button class="waves-effect waves-light btn-small" type="submit" value="Save Profile">Speichern<i class="material-icons">submit</i>
					<br>
				</button>
				<a href="/" class="waves-effect waves-light btn-small">Zurück</a>
			</form>
		</div>
	</div>
</div>
</body>
{% endblock %}